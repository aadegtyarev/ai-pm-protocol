# Execution State

- **Status:** TWO features on this branch, ship together (PM: "отгрузить вместе"). Feature 1 `review-engine-selection` = DONE (Pass-1 approve/DoD pass, Pass-2 no defects, stamped, committed). Feature 2 `audit-scope-menu` = CODER DONE (all 4 scenarios landed in `.claude/commands/pm-audit.md`, committed; hooks.sh 73/73). Then joint review + ship as one PR.
- **Decision authority:** `autonomous` (project-wide, `.ai-pm/decision-authority.md`) — routine procedural gates announce-and-proceed; merge/ship stays manual. **Product forks go to the PM** (PM directive 2026-06-05: "продуктовые решения со мной"). Conversation language: Russian (PM directive 2026-06-05).
- **Task:** `review-engine-selection` — remove the `code-review-orchestrator` hook deny (it becomes a legitimate engine), keep per-diff Pass-2 on the built-in `/code-review` by protocol routing, and make the `/pm-audit` whole-codebase smell sweep prefer the orchestrator (built-in `/code-review ultra` fallback + `WB_REVIEW_ORCHESTRATOR=off` switch). Plan: `doc/features/review-engine-selection_plan.md`.
- **Branch:** `feature/periodic-codebase-review`.
- **Done (planning):** read context; found #269 (periodic whole-codebase review) already shipped by the smell/hygiene slice v2.27.0 → this plan is the engine-selection residue (#372/#378) + the PM-directed deny removal; PM decided the product fork (unblock orchestrator, protocol routes per-diff to built-in); plan written; advocate gate determined exempt (backend-only, all scenario subjects non-human, no Product Contract — dogfood exception); architect review skipped (engine-rule home dictated by the established single-source pattern).
- **Done (coder, all 5 items landed):**
  1. ✓ `.claude/settings.json` — removed the `wb-development:code-review-orchestrator)` `case` arm + the `WB_ALLOW_REVIEW_ORCHESTRATOR` escape guard; the 7 role-duplicator arm verified byte-identical to `main` (only line 55 changed; JSON valid).
  2. ✓ `tests/hooks.sh` — rewrote the 3 orchestrator cases: orchestrator → NOT denied (let-through `pass`); dropped the obsolete flag-escape case + its `export`/`unset`; kept `wb-development:coder still denied`; rewrote the comment block. 73/73 pass.
  3. ✓ `workflow/review-typology.md` — added the single-source engine-selection rule (per-diff → built-in; whole-codebase → orchestrator-preferred → built-in `ultra` fallback → `WB_REVIEW_ORCHESTRATOR=off` forces fallback; reframes deny-review-orchestrator).
  4. ✓ `.claude/commands/pm-audit.md` `## Technical quality` — smell sweep engine-selected per `### Review typology` by reference (intro + step 3); all existing sweep behavior preserved.
  5. ✓ `workflow/enforcement.md` — one-line note: orchestrator is a legitimate engine off the deny-list; named deny-list is exactly the 7 role-duplicators.
- **Done (pm-architect, Docs-to-update — both landed):** `doc/architecture.md` — added the `### Review-engine selection` decision record (engine split + supersedes `deny-review-orchestrator` v2.25.1 + security framing: settings.json is a security-relevant hook, deny removal was a PM decision); `README.md` — replaced the "отключён по умолчанию" section with the engine-selection note (per-diff → built-in; sweep → orchestrator-preferred → `/code-review ultra` fallback; `WB_REVIEW_ORCHESTRATOR=off`), dropped the obsolete `WB_ALLOW_REVIEW_ORCHESTRATOR` launch-flag instructions. 2 commits on the branch.
- **Done (coder, `audit-scope-menu`, all 4 scenarios landed):** `.claude/commands/pm-audit.md` — renamed `## Auto-scope decision` → `## Scope decision` with 4 steps: (1) compute threshold judgement (unchanged logic, now the menu default), (2) route on initiator — system-initiated announces-and-proceeds in both modes, PM-initiated goes to menu, (3) explicit scope skips the menu (Full still surfaces depth), (4) the upfront Quick/Full `AskUserQuestion` menu with the judgement pre-selected, depth folded into Full. Added a "No double-prompt" guard to `## PM-facing flow` (post-diff "run full now?" fires only when Quick/diff actually ran). Updated the Hard-rules scope line and the internal `## Technical quality` cross-ref (`Auto-scope` → `Scope decision, step 1`). 1 commit.
- **Touched files:** `.claude/settings.json`, `tests/hooks.sh`, `workflow/review-typology.md`, `.claude/commands/pm-audit.md` (touched by BOTH features — engine-selection `## Technical quality` + scope-menu `## Scope decision`/`## PM-facing flow`), `workflow/enforcement.md` (coder, 3 commits total), `doc/architecture.md`, `README.md` (pm-architect, 2 commits — `audit-scope-menu` arch handoff still pending) — all on `feature/periodic-codebase-review`.
- **Next step:** review loop for both features (`pm-plan-checker` Pass 1 → `code-review` Pass 2 — `.claude/settings.json` is a security-relevant hook, review accordingly). `audit-scope-menu` Docs-to-update: `pm-architect` post-coding addition to `doc/architecture.md` review-dimensions/`--scope` record.
- **Validation:** `tests/hooks.sh` green (the deterministic hook half); editorial/plan-compliance for the prose half (markdown-prose repo, validation-by-use).
- **Note (state-archive home):** the prior `orchestrator-read-discipline` state + archive were carried onto this branch at Step 0 (commit on `feature/periodic-codebase-review`), per the known state-archive-home gap.
