# Execution State (archived)

- **Shipped:** `deny-review-orchestrator` (v2.25.1, PR #217 merged 2026-06-05) — extended the shipped `.claude/settings.json` routing hook to deny `wb-development:code-review-orchestrator` (it would auto-intercept the protocol's own `/code-review` Pass-2) with a surgical per-skill env-escape `WB_ALLOW_REVIEW_ORCHESTRATOR=1` (bypasses only that one deny, not other hooks). + 3 hook tests (74/74) + README install note. Ships downstream via the submodule. PM-directed; escalated from /pm-fixup (behavior change). Plan-check approve; code-review NO defects (security-sensitive deny-bypass independently verified scoped via mutation test). Harness raised a self-modification security warning — reviewed, authorized (PM chose deny+env-escape), correctly scoped.
