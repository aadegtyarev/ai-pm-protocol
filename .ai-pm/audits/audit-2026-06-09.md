# Protocol audit — 2026-06-09

## Summary

The protocol is in substantially healthy shape: all 5 slices of the `doc-frugality` feature are reviewed, stamped, and merged with clear ADR graduation. The main blocking gap is a missing plan file for the `delegation-role-integrity` enforcement change — a meaningful behavior change that bypassed the plan gate. Frugality dimension findings are the load-bearing outputs of this audit: `doc/architecture.md` at 613 lines / 224 KB with 45 ADRs under one `##` heading is the dominant scale-guard target, and `.ai-pm/state/current.md` at 69 KB with a single 25 KB line (line 7) is an extreme bloat case the feature just shipped specifically to fix. The `doc-frugality` migration on this repo itself is the priority action — its own MIGRATIONS.md entry now defines the procedure.

---

## Contract check

Fill this table for EVERY feature in the inventory before writing Blocking/Notes.

| Feature | Scenario 1 subject | Needs contract | Contract file |
|---|---|---|---|
| doc-frugality (slices A–E) | `pm-architect` / `pm-stack-researcher` / `pm-coder` (system agents) | no | n/a |
| bootstrap-write-loss-guards | "any caller (orchestrator or subagent)" (system) | no | n/a |
| delegation-role-integrity | "the model (orchestrator)" (system) | no | n/a |
| opencode-compact-reviewer | "the per-diff Pass-2 engine" (system) | no | n/a |
| orchestrator-anti-corner-cutting (p0/p12) | "orchestrator" on subagent failure (system) | no | n/a |

Rule: subject is a human role (integrator, operator, user, admin, developer…) → `yes`. Subject is a system/process/file → `no`. No `yes + MISSING` rows; no blocking from this check.

---

## Blocking

1. **`doc/features/` — `delegation-role-integrity` merged with no plan file.** The `merge(core): role-delegation integrity` commit (`9e683154`, 2026-06-08) introduced a meaningful enforcement change: generalized the `pm-*-not-wb-*` invariant to "never a generic built-in (`general`/`build`/`plan`) as a protocol-role substitute", wired it in `WORKFLOW.md`, `workflow/enforcement.md`, `workflow/roster.md`, and the OpenCode plugin. No plan file exists in `doc/features/`. The `.ai-pm/reviews/delegation-role-integrity_review.md` exists (stamped Pass-2), so the review trail is present, but `doc/features/delegation-role-integrity_plan.md` is missing. **Why it matters:** a merged feature without a plan bypasses the plan-gate that ensures scenario coverage is on record, and leaves no basis for the auditor graduation check (dimension 5 / C.9). **Remediation:** retroactive `/pm-plan delegation-role-integrity` (write what was built — scenarios, scope decision, architecture pointer to the `### Orchestrator anti-corner-cutting` ADR where the generic-builtin deny is now referenced).

2. **`doc/architecture.md` — no decision record for `delegation-role-integrity`.** The role-delegation integrity feature changed `WORKFLOW.md`, `workflow/enforcement.md`, and `workflow/roster.md` but did not produce an ADR in `doc/architecture.md`. The closest ADR (`### Orchestrator anti-corner-cutting`) does not mention the generic-builtin deny-list addition (`GENERIC_BUILTIN_DENY`). This is a graduation-check (C.9) blocking finding: a durable decision (structural enforcement of "never a generic built-in as a protocol seat") has no permanent home. **Why it matters:** without an ADR, the decision is invisible to future maintainers and auditors and cannot be referenced by downstream projects. **Remediation:** spawn `pm-architect` to add a brief decision record (either a new `### Role-delegation integrity: generic built-in deny` ADR, or a targeted amendment to `### Orchestrator anti-corner-cutting`).

3. **`doc/product-map.md` — `doc-frugality` feature missing from map.** The product-map was last regenerated on 2026-06-08; `doc-frugality` completed 2026-06-09 and has not been added to the Infrastructure bucket. 74 plan files exist; the map covers 73. **Why it matters:** the map is the PM-facing generated view; a missing entry means the protocol's own product record is stale. **Remediation:** regenerate `doc/product-map.md` via the Product map generation procedure (orchestrator-owned, idempotent overwrite-from-source — `doc-frugality` surfaces under `## Infrastructure (no user-facing contract)`).

---

## Notes

### Dimension 1: Artifact completeness

4. **`.ai-pm/reviews/doc-frugality_review.md` absent — accepted by slice structure.** The `doc-frugality` plan has 5 per-slice review files (`doc-frugality-sliceA_review.md` through `sliceE_review.md`), each stamped `## Code review: 2026-06-09 — passed`. No top-level `doc-frugality_review.md` exists. This is consistent with the plan's stated review structure ("each slice ships on its own sub-branch → review → merge-back") and all 5 pass-2 stamps are present. The note is informational: the slice convention satisfies the pass-2 stamp requirement, but `pm-auditor` will pattern-match `<topic>_review.md` on future audits and flag the absence unless a convention note is recorded. **Remediation:** either create a `doc-frugality_review.md` as a thin pointer to the 5 slice files, or add this slice-review pattern to the accepted-cohort note in backlog.

5. **`.ai-pm/reviews/` — `bootstrap-write-loss-guards` has `_plan-check.md`, not `_review.md`.** The file is `bootstrap-write-loss-guards_plan-check.md` with a valid stamped `## Code review: 2026-06-06 — passed`. The naming convention differs from `<topic>_review.md` and may confuse future audits' pattern match. **Remediation:** no action required if the naming is intentional (plan-check vs full review); document in the accepted-cohort note or rename for consistency.

### Dimension 4: Contract currency

6. **All 10 contracts present and consistent with their `Built by:` tables in `doc/product-map.md`.** No stale contract findings. (Structural note: contracts' `## User value` / `## Out of scope` sections use plain product language with no wire-token shapes detected — the two-layer migration appears clean.)

### Dimension 5: Docs currency

7. **`doc/product-map.md` format is value-first (current).** No `Guarantees:` or `Что даёт:` labels detected. Map leads with `- **User value:**` blocks. Format-refresh note does not apply.

8. **`doc/product.md` carries English funnel headers** (`## Why this exists`, `## What it does today`, `## Documents`, `## Features`). Structure pass.

9. **`README.md` has no `## What it does` capability list** (old-template signal absent). Has `## Установка` (install section), `## License`, and `doc/product.md` pointer on line 5. All canonical beats present. README install command (`git submodule add git@github.com:aadegtyarev/ai-pm-protocol-uni.git .ai-pm/tooling`) matches the `## Integration contract` in `doc/architecture.md`. No README findings.

10. **`doc/user-journeys.md` (`doc/` not `docs/`) covers all 6 journeys including the new cross-model and enforcement journeys.** No missing journey for implemented user-facing features detected. (This repo has no user-facing runtime feature; all journeys cover the protocol's own flows — journeys 1–6 are current.)

11. **`doc/architecture.md` `## File layout` section is current** — lists the dual-harness layout (`src/`, `gen/`, `.claude/`, `.opencode/`, `doc/_templates/`) and the new frugality-related paths. No significant component missing from the layout table.

12. **`workflow/doc-style.md` graduation-targets table uses `docs/architecture.md` / `docs/stack-notes.md` paths** (the downstream convention) while this repo uses `doc/`. This is intentional (the table describes the downstream project's homes, not this repo's own paths) but creates a slight documentation asymmetry for this repo's dogfood use. Note only — not a finding per the no-prose-policing rule, but worth clarifying in a future `pm-architect` pass.

### Dimension 6: Frugality (LOAD-BEARING — self-migration scoping)

13. **`doc/architecture.md` — SCALE-GUARD TRIGGER (size + node-density).** 613 lines, 224 KB, 45 `###` ADR sub-sections all under a single `## Architectural decisions` heading. This is well past the scale-guard threshold — the doc does not fit in one readable pass. The lost-in-the-middle risk is high: the 45-ADR sequence starts at line 32 and runs to line ~466 (434 lines of decision records under one `##`). The ADRs themselves were written to the new plain-language / fact-first discipline (the `### Keep only what's actually used…` ADR is a good example), but the accumulation means any single ADR is buried. **Why it matters:** this is the central reference document; an agent reading it must ingest 224 KB to locate one ADR, and the F-pattern / Lost-in-the-Middle effect means load-bearing decisions buried in the middle are under-read. **Remediation:** spawn `pm-architect` to shard `## Architectural decisions` into a thin-core + on-demand sub-files using the progressive-disclosure pattern — a thin index of ADR titles + one-line summaries in the main `doc/architecture.md`, with full ADR bodies moved to `doc/decisions/<name>.md` topic files (same shape as `WORKFLOW.md` → `workflow/*.md`). This is the priority shard.

14. **`doc/stack-notes.md` — size smell.** 429 lines, 110 KB. 14 `##` component sections. The file is well-structured (one `##` per component, limited `###` nesting), but at 110 KB it is a heavy on-demand read. Node density is acceptable (14 `##` sections, no excessive `###` fragmentation). No scale-guard trigger (not "by a wide margin past target"), but noting for the self-migration worklist as a compaction candidate. **Remediation:** `pm-architect` compaction pass — each component entry to the shortest form that preserves the cited idioms and gotchas.

15. **`MIGRATIONS.md` — size smell.** 183 lines, 36 KB. Accretes migration procedures end-over-end. Each new migration version adds a block. Currently 7 version entries plus the detection section. The file is not yet at scale-guard threshold but will grow with each version. **Note for self-migration worklist.**

16. **`.ai-pm/state/current.md` — EXTREME BLOAT CASE.** 45 lines, 69 KB. Line 7 is a single 25 KB line — a continuous accumulation of slice-by-slice progress notes for the entire `doc-frugality` + `orchestrator-anti-corner-cutting` + OpenCode effort, spanning multiple features. This is the exact anti-pattern `doc-frugality` was built to prevent. Under the O(1) lifecycle (Slice D), the state file should reset to `Status: idle` after each feature merge, with durable bits graduated. The bloat predates the O(1) lifecycle landing; the file was in active use during the build and was not distilled on feature completion. **Why it matters:** a 25 KB state line cannot be read in context by an agent's context window efficiently; the resume-pointer function the file is supposed to serve is defeated. **Remediation:** `pm-architect` distillation pass — reset `current.md` to idle now that `doc-frugality` is COMPLETE; carry the one-liner summary from the state file's starred completion line into the ADR; confirm all durable bits graduated (already done per ADR + MIGRATIONS.md + 5 review files).

17. **`.ai-pm/reviews/` — file-sprawl (note, not blocking; pre-distillation cohort).** 84 review files and 26 arch note files under `.ai-pm/reviews/` and `.ai-pm/arch/`. Under the new O(1) lifecycle (Slice D, just merged), per-feature review files for ALREADY-MERGED features should collapse to git — these files are pre-distillation process evidence from before the model landed. The backlog's accepted OpenCode cohort (6 features) explicitly notes retroactive `_review.md` authoring before `uni` release. This is the most significant sprawl in the repo by file count. **Remediation:** this is the primary target of the self-migration via `doc-frugality` MIGRATIONS procedure. Not an individual remediation — batch the collapse as part of the downstream migration (see `## Self-migration worklist` below). **Note:** by the mixed-project tolerance rule, these files are OLD-MODEL presence (durable bits live in them), so graduation is satisfied per the tolerance rule — not blocking.

18. **`.ai-pm/audits/` — audit file accumulation.** 5 audit files (2026-05-30 through 2026-06-08). Under the O(1) model, prior audit artifacts are process evidence. They serve as the diff-scope cutoff for incremental audits and the accepted-cohort record. No action now — this is a smaller sprawl surface.

19. **`WORKFLOW.md` — 83 lines, 13 KB.** Lean. Progressive-disclosure working as intended. No size smell.

20. **`doc/user-journeys.md` — 147 lines, 17 KB.** Within range for a journeys doc. No scale-guard trigger.

21. **`doc/product-map.md` — 247 lines, 21 KB.** Generated file; expected to grow with contracts/features. No action.

22. **`README.md` — 251 lines, 21 KB.** The install section is long (OpenCode preview subsection adds ~20 lines). Not a hard-cap violation. Acceptable.

### Graduation check (C.9 — for recently-merged features)

23. **`doc-frugality` — graduation CLEAN.** ADR `### Keep only what's actually used; throw the rest away` present in `doc/architecture.md`. MIGRATIONS.md carries doc-frugality migration entry and state-archive-home supersede entry. `.ai-pm/backlog.md` carries the behavioral follow-up (orchestrator obeys pm-comms no-jargon rule). 5 stamped review files present as per-slice process evidence (old model) — durable knowledge also in ADR (new model). Presence-in-any-durable-home satisfied. Clean.

24. **`bootstrap-write-loss-guards` — graduation CLEAN.** Decision record in `doc/architecture.md` `### Bootstrap write-loss guards: a destructive-Write deny plus a bootstrap doc-snapshot`. Stamped plan-check review (`## Code review: 2026-06-06 — passed`). Clean.

25. **`orchestrator-anti-corner-cutting` (p0/p12 + core hoist) — graduation CLEAN.** Full decision record in `doc/architecture.md` `### Orchestrator anti-corner-cutting`. Two stamped review files (`orchestrator-anti-corner-cutting-p12_review.md`, `orchestrator-anti-corner-cutting-p0-core_review.md`). Clean.

26. **`delegation-role-integrity` — graduation PARTIAL (ADR missing — see Blocking item 2).** The enforcement change is in `WORKFLOW.md`/`enforcement.md`/`roster.md` and covered by the plugin. The decision is NOT in `doc/architecture.md`. The review file is stamped. This is the blocking graduation gap.

27. **`opencode-compact-reviewer` + OpenCode sub-slices — graduation CLEAN (per accepted cohort).** Decision record absorbed into `doc/architecture.md` `### OpenCode compact one-pass reviewer…`. Per the accepted-cohort backlog entry (2026-06-08), the sub-slice `_review.md` absence is accepted; retroactive review authoring deferred to `uni` release. Clean under accepted-cohort tolerance.

---

## What looks healthy

- All 5 `doc-frugality` slices have proper Pass-1 + Pass-2 stamps and complete ADR graduation. The feature delivered what it promised.
- All 10 product contracts are present, value-first structured, and consistent with the product-map.
- The `delegation-role-integrity` review is stamped (Pass-2 `2026-06-08`) — the code is sound; the protocol-artifact gap is the only issue.
- `doc/product.md` and `README.md` meet all canonical-beats checks.
- Progressive-disclosure is working (`WORKFLOW.md` thin, `workflow/*.md` set lean and focused).
- The MIGRATIONS.md doc-frugality migration entry is present — the protocol can migrate downstream projects.
- The `doc-style.md` frugality discipline is well-formed: plain-language / fact-first / targets-not-gates shape is clean.

---

## Priority order for remediation

1. **Blocking — retroactive plan for `delegation-role-integrity`** (`/pm-plan delegation-role-integrity` — write the plan for what was built, then spawn `pm-architect` for the ADR amendment).
2. **Blocking — ADR for `delegation-role-integrity`** (spawn `pm-architect`; can combine with step 1's pm-architect handoff).
3. **Blocking — regenerate `doc/product-map.md`** (orchestrator runs the Product map generation procedure, adds `doc-frugality` to Infrastructure; low effort, high visibility).
4. **Scale-guard — shard `doc/architecture.md`** (spawn `pm-architect`; largest effort, highest impact — the `## Architectural decisions` section with 45 ADRs needs progressive disclosure).
5. **State file distillation** — reset `.ai-pm/state/current.md` to idle; durable bits already graduated.
6. **Reviews/arch sprawl** — batch collapse per the self-migration procedure (see worklist below); can be done incrementally.

---

## Self-migration worklist

Ordered, concrete list of what "rewriting our own project under the new doc-frugality protocol" entails. Derived from frugality findings and graduation check above.

### Phase 1 — Immediate protocol compliance (blocking + state)

1. **Retroactive plan + ADR for `delegation-role-integrity`** (see blocking items 1 and 2). Run `/pm-plan delegation-role-integrity` retroactively, then `pm-architect` adds the ADR.
2. **Regenerate `doc/product-map.md`** to add `doc-frugality` to Infrastructure bucket (orchestrator, ~5 min).
3. **Reset `.ai-pm/state/current.md` to idle.** The `doc-frugality` feature is COMPLETE (all 5 slices `ee091f6`/`5ac8e18`/`d8f0eac`/`dae96c2`/`c518504` merged). The state file's massive line 7 (25 KB) is pure process evidence. Durable bits are already in `doc/architecture.md` (ADR), `MIGRATIONS.md` (supersede entries), and the 5 review files. `pm-architect` resets to: `Status: idle. Last feature: doc-frugality (complete, all 5 slices). Branch: feature/opencode-harness-support (integration, not pushed).`

### Phase 2 — Standing-doc distillation (core migration)

4. **Shard `doc/architecture.md` `## Architectural decisions`** (45 ADRs → thin index + `doc/decisions/*.md` on-demand topic files). This is the load-bearing migration — it implements the progressive-disclosure pattern on the repo's own heaviest doc. `pm-architect` performs the shard: create `doc/decisions/` directory; one file per ADR group (e.g., `doc/decisions/enforcement.md`, `doc/decisions/review.md`, `doc/decisions/dual-harness.md`); the main `doc/architecture.md` `## Architectural decisions` becomes a table of `| Decision | File | Date |` entries pointing to the topic files. Expected outcome: `doc/architecture.md` collapses from 613 → ~200 lines; each decision file is ~1–3 screens.
5. **Compact `doc/stack-notes.md`** (429 lines → target ~200 lines). `pm-architect` compaction pass: each component entry to the shortest form that preserves the cited idioms and gotchas; any "Background" / "Why" narrative collapses to one sentence or is deleted if the cited source carries it.
6. **Distill MIGRATIONS.md** (183 lines → target ~120 lines). The detection section is lean; the migration procedures accumulate. Consider moving full migration procedures into versioned `doc/migrations/vX.Y.md` files with one-line summaries in `MIGRATIONS.md`.

### Phase 3 — Evidence collapse (file-sprawl)

7. **Collapse `.ai-pm/reviews/` and `.ai-pm/arch/` to git** per the doc-frugality migration procedure. For each merged feature's review/arch file: verify graduation (durable bits in ADR/contracts/backlog/stack-notes — most already done), then remove the file from the working tree (git retains the history). Priority order: start with the oldest (pre-protocol-migration cohort: `template-v2`, `contract-centric-product-map`, `diagnostic-probe-mode`, `protocol-builtins-realignment`) then the large OpenCode-effort files (`.ai-pm/arch/opencode-harness-support_arch.md` is the largest single evidence file at likely ~100+ KB). Respect the mixed-project tolerance: do not collapse a file whose durable bits are NOT yet in a durable home.
8. **Remove `.ai-pm/audits/` pre-migration files** once the accepted-cohort record in `backlog.md` is the single source. Or leave them — the files are small (13–21 KB each) and serve as historical record; the diff-scope cutoff reads the most recent by name. Low priority.

### Phase 4 — OpenCode feature completion

9. **Retroactive `_review.md` files for the 6 accepted-cohort OpenCode sub-slices** (per backlog item: `opencode-harness-support`, `harness-neutral-prose`, `context-leanness`, `per-operation-effort-tiering`, `opencode-orchestrator-primary`, `protocol-integrity-and-stack-literacy`) — before the `uni` release if full pre-release rigor is wanted. The accepted-cohort note names this as deferred, not dropped.
10. **Piece 3 of `orchestrator-anti-corner-cutting`** (chat-hook route-reminder for OpenCode) — re-run the spike where `opencode run` completes a turn; currently blocked by the SQLite session-insert hang.
